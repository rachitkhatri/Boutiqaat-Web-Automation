# Browser automation
playwright

# Test runner
pytest

# Playwright pytest plugin — provides browser/page fixtures
pytest-playwright

# HTML test report: pytest --html=report.html
pytest-html

# HTTP client — used for REST API registration in registration_page.py
requests

# Load .env files for secrets (optional)
python-dotenv
pytest-json-report

# Auto-retry failed tests (handles flaky web automation failures)
pytest-rerunfailures
